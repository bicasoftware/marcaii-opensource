<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:ads="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <android.support.design.widget.TabLayout
        android:id="@+id/tabLayoutEmpregos"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scrollbars="horizontal"
        ads:layout_scrollFlags="scroll|enterAlways"
        ads:tabBackground="@color/primary"
        ads:tabSelectedTextColor="@color/icons"
        ads:tabTextColor="@color/primary_light" />

    <android.support.v4.view.ViewPager
        android:id="@+id/viewPagerEmprego"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        ads:layout_behavior="@string/appbar_scrolling_view_behavior" />

</LinearLayout>